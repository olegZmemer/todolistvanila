# Simple TODO List

**1.2 Version**
 
 Run *npm install*
     *ncu*
     *ncu -u*
     *npm install*
     *npm run build* if you want to see produaction vers.
     *npm run dev* if you want to see dev vers.